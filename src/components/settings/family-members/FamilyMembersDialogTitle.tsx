import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export const FamilyMembersDialogTitle = () => {
  return (
    <DialogHeader>
      <DialogTitle>Family Members Management</DialogTitle>
    </DialogHeader>
  );
};